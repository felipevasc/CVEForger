import React from 'react';
import { Typography, Container } from '@mui/material';

const RealizarPocPage: React.FC = () => (
  <Container>
    <Typography variant="h4" component="h1" gutterBottom>
      Realizar PoC Page
    </Typography>
    <Typography variant="body1">
      Conteúdo da página Realizar PoC.
    </Typography>
  </Container>
);

export default RealizarPocPage;
